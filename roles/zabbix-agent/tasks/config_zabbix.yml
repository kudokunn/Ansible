- name: config zabbix agent
  lineinfile:
    dest: /etc/zabbix/zabbix_agentd.conf
    regexp: "{{ item.regexp }}"
    line: "{{ item.line }}"
  with_items:
    - { regexp: 'Server=*', line: 'Server={{ip_server}}' }
    - { regexp: 'ServerActive=*', line: 'ServerActive={{ip_server}}' }
